import { members } from "@constants/members";
import { Avatar } from "@components/Avatar";
import { Flex } from "@chakra-ui/react";

# 열정넘치는 쏙쏙쑥쑥 스터디원

<Flex gap="50px" flexWrap="wrap" mt="50px">
  {members.map((member) => (
    <Avatar key={member.ghId} {...member} />
  ))}
</Flex>
