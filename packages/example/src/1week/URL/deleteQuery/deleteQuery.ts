/**
 * 출제자: 예진님 (yejineee)
 * URL의 쿼리스트링 관련 유틸 함수를 계산 을 최대한 이용하여 만들기
 *
 * 4. `deleteQuery` 함수: 현재 URL에서 주어진 key에 해당하는 모든 값을 제거
 */

export const deleteQuery = (originURL: string, key: string) => {
  return originURL
}
