type Sumber = string | number

/**
 * 출제자: 예진님 (yejineee)
 * URL의 쿼리스트링 관련 유틸 함수를 `계산`을 최대한 이용하여 만들기
 *
 * parse 함수: url의 쿼리 스트링을 객체로 변환해주는 함수
 */
export const parse = (
  queryString: string,
): Record<string, Sumber | Sumber[]> => {
  return {}
}
